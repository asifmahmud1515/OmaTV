# Downloaded channel playlists + flattened variant cache are regenerated
# locally, never committed (they roll daily upstream).
channels/*.m3u
channels/.flat/
channels/.refreshed
channels/custom/
channels/custom.json

__pycache__/
*.pyc